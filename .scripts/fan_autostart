# To start at boot run sudo crontab -e and add the following:
# @reboot /home/steven/.scripts/fan_autostart

sleep 5
#liquidctl set ring color spectrum-wave
liquidctl set ring color fixed 0bf0f0
liquidctl set fan speed 35
